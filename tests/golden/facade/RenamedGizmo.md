# Struct `facade::RenamedGizmo`

_Reexported from_ `dep::Gizmo`.

_Defined at_ `src/lib.rs:25`

A gizmo that the facade reexports under a different name.

## Definition

```rust
pub struct RenamedGizmo;
```
